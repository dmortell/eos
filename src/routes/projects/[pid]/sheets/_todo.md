
1. Package sheets into drawing sets  ✅ DONE
   Sheets/Packages toggle on the list; PackageManager with two-list membership (add/remove/reorder by
   buttons or drag); per-package Print → /sheets/packages/[pkgId]/print renders every sheet one per
   page. Data: projects/{pid}/sheetPackages. (Page number in titleblock still TODO if wanted.)
See the packages tool (src\routes\projects\[pid]\packages) and drawings tool (src\routes\projects\[pid]\drawings)

A toggle button in our sheets list view switches to package manager which lets use add/edit a list of packages/drawing sets.
A print button on each item in the list opens a print preview with all sheets in the package, one per paper page, that can be printed to PDF or hardcopy. (we might want the page number in the titleblock at a later date).
On selecting a package, show 2 lists, one with sheets in the package and one with remaining sheets. Let the user select drawings in one list and drag them (or use buttons) to move them to the other list.
Allow user to reorder sheets in the list.

Try to reuse the existing sheet list code.

2. Renumber sheets  ✅ DONE
A renumber tool (maybe a menuitem in a ... button to the right of the New Sheet button) that lets renumber selected or clicked sheets in the list in order continuing from the previous or first selected sheet.
Preserve any prefix text the user may have entered in the label.
   → Row checkboxes + select-all; "⋮" menu ▸ "Renumber selected (N)". Sequential from the first
     selected sheet's number (or the sheet before it / 1); each sheet's prefix + zero-pad preserved.

3. Revisions
Like our exiting tools, our sheets/packages will need to track published packages and revisions.
The sheets list should show the latest version by default, but maybe a dropdown arrow in the actions column to select an old version for viewing or allow reverting it to be the latest version for further updates.

4. Copy/paste from other projects


a. Layers
  * Default layers are good, but allow user to add/edit custom layers (maybe categorized under one of our default layers), and select a current layer for new objects being added
  * Allow object to be moved to other layers
  * If layers are categorized, we could warn (with toast) if object is being added to wrong category of custom layer (as its easy for user to forget to change layers), or simply add object to default layer and let user move them later.

b. Outlets
  * Upload/insert PDF floorplan
  * need an Outlets list, and export to Excel
  * Need a tool to Auto-Renumber selected or clicked outlets (after outlets are added or removed). Allow a user defined prefix for outlet labels. Highlight duplicate outlet numbers as errors
  * List of racks (from rack elevations). Allow racks to be dragged to floorplan. (Consider disabling adding new racks in the outlets tool? Or do we allow rack elevations to be generated by selecting an elevation/section in the floorplan and using a Section annote to link them?)
  * Outlets and trunks created in sheets outlets tool appear in old tools with Firestore ID as the label, check this is purely because user has not yet added a label (actually, there is no input for entering the trunk label, so auto-gen a label like our old tool does).
  * Refactor all rack property editors (Sheets, outlets tool, racks tool) into one, keeping all advanced functionality
  * Outlets should be linked back to patch frames using the frames tool
  * Trunks should be linked to one or more patch frames/racks. Outlets secondary trunks should link to the nearest main trunk that connects to the correct frame/rack

c. Trunks
  * Change the Shape in trunk properties editor to 2 toggle buttons. Same for trunk location (simplify to: floor, ceiling, wall)
  * Sheets trunk props should include type, color
  * Need a list of trunks, just to select (old tool allows toggling visibility, but probably should use layers for this)

d. Elevations
  * The select dropdown for selecting a rack works, but consider adding a rack list (that allows the order to be changed by dragging selected racks(s) in the list), and a button to add new rack. The Devices button opens a device list window, maybe add a Racks button that opens a racks window?
  * The custom device form should probably be a modal dialog (since the lsit will get long) that can be opened from the list too for editing devices
  * Should we link rack elevations to section views in floorplans, for section/elevation references? Probably yes, as we dont want to be limited to rack rows, we also want wall elevations for server room wall panels and meeting room AV equipment
  * Would like trunks to be drawable in elevations (horiz and vertical) to give more detail to the trunks drawn in floorplans

e. Viewports
  * Make border property a checkbox for thin/none now. Prefer checkboxes to be to the right of the prop label
  * In property editors, make enter key move to the next input field, and shift+Enter move to previous (except textareas)
  * If we have multiple vps on a sheet, we should number them (allowing renumbering if user changes the order/positions), for use in references. Include number in the label if defined
  * Autocad viewports show a down-triangle in the vp for selecting the scale (), and a square for something I forget. Maybe add these to our vp toolbar too.


f. Annotates
  * We have a lot of buttons for annotes, plus a dropdown for Symbols. Maybe cleanup this UI using a group toggle button (that toggles between the Select button and the annote button), and a dropdown chevron to let user select the desired annote. If space permits, keep just a few of the most recently used annotes as more toggle buttons in the group.
  * Add an Image annote that allows pasting a url or base64 encoded or uploading image to assets folder when an image is pasted (or even dragged?) into the view.

g. Frames
  * Frames is for linking outlets and rack ties to patch panels to get port labels.
  * Frames is probably not needed in a viewport (unless we want a titleblock?). Leave it as a standalone tool, but they should appear in the drawings/sheets list for adding to packages. But should it be rendered in html/PDF, or as Excel?
  * Outlets can be assigned to a room/row (or rack?) and given a usage. Then blocks of ports on frame panels can be assigned usage to auto-allocate ports by usage. See existing frames tool
  * To show port labels in a viewport, I'm thinking of adding a "frame" (or "ports" may be better?) option to the View setting of the viewport. Then the user can zoom in on a rack to get it large enough to view the labels in an A3 portrait page.
  * Rack ties specify the B-end for the panel/ports on this frame.
  * The Generate button is the existing frames tool is for auto-generating outlets on large floorplans rather than placing outlets one by one. We probably dont need the auto-gen function, but the outlet list is useful for bulk-setting outlet usage, room (outlet description) and types (high-level/low-level, etc)
    * The list could highlight duplicate labels, or gaps in numbering (a colored horizontal line when not sequential)
    * Is the outlet list needed in both the Frames view and the Outlets floorplan view?
    * Or do we need the list at all? The toolbar that appears when a block of ports is selected might work witout it. Maybe just leave it permanently visible or in a floating window, but disabled when not ports are selected?
    * The list in the resizable sidebar works well. I havent needed the vertical arrangement at all so we could drop those toggle buttons.
    * The Export to Excel and the Settings in our original tool are great and should be available from Sheets too.
    * I dont think we need to keep logs for port assigns (logs or change history is more important for outlet/device changes)

h. Patching
  * Patching tool is primarily managing patch lists, with a visual editor to help see connections.
  * We dont need to render to a viewport, but ideally we should be able to include the Excel list in packages (rendered to PDF)
  * Keep this tool as is.
  * Consider adding a schematic view of the hops between A-end and B-end, as there may be multiple patchcords between panels and ties and endpoints. (Sounds similar to the hopis in riser cable routes)

